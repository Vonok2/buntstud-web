### ✅ README.md (скопируй и вставь в репозиторий)


# 🎨 BuntStud Web — Креативный сайт для конкурса web-мастерства

Этот проект создаётся в рамках конкурса **креативных web-сайтов "БунтСтуд"**.  
Цель — разработать сайт образовательного учреждения с упором на креативность, эмоции и современный UI/UX.

---

## 🚀 Стек технологий

Проект создаётся **без фреймворков и CMS**, вручную:

| Область | Технологии |
|---------|------------|
| Разметка | HTML5 |
| Стили | CSS3 *(модульная структура, отдельные файлы для страниц)* |
| Сценарии | JavaScript (Vanilla JS) — без библиотек |
| Макеты | Figma (UI/UX дизайн сайта) |
| Контроль версий | Git + GitHub |

---

## 📁 Архитектура проекта

```
/project-root/
│
├─ /assets/                # изображения, иконки, шрифты
│   ├─ /img/
│   ├─ /icons/
│   └─ /fonts/
│
├─ /css/
│   ├─ /common/            # общие стили
│   │    ├─ reset.css      # сброс стилей
│   │    ├─ variables.css  # CSS-переменные (цвета, шрифты, отступы)
│   │    ├─ header.css     # стили хедера
│   │    └─ footer.css     # стили футера
│   │
│   ├─ main.css            # стили главной страницы
│   ├─ about.css           # стили страницы "О колледже / О проекте"
│   ├─ programs.css        # стили страницы "Специальности"
│   └─ contacts.css        # стили страницы "Контакты"
│
├─ /js/
│   ├─ slider.js           # слайдер на главной
│   ├─ menu.js             # бургер-меню, адаптив навигации
│   └─ main.js             # прочий функционал
│
├─ /pages/
│   ├─ about.html
│   ├─ programs.html
│   └─ contacts.html
│
└─ index.html              # главная страница
```

---

## 🔧 Как подключаются стили на странице

В начале каждого файла стилей страницы:

```css
@import url("./common/reset.css");
@import url("./common/variables.css");
@import url("./common/header.css");
@import url("./common/footer.css");

/* дальше — уникальные стили страницы */
````

Так мы избегаем огромного style.css и делаем проект модульным.

---

## 🧑‍🤝‍🧑 Совместная работа с GitHub

### ✅ 1. Как скачать проект (для напарника)

Открыть терминал → перейти в папку, куда хотите скачать проект:

```sh
git clone https://github.com/Vonok2/buntstud-web.git
```

Зайти в скачанную папку:

```sh
cd buntstud-web
```

---

### ✅ 2. Как создать свою ветку (чтобы не ломать main)

```sh
git checkout -b feature/header
```

> Название ветки = то, над чем работаешь (`feature/header`, `feature/footer`, `fix-slider`).

---

### ✅ 3. Как отправить изменения в репозиторий

После внесения изменений:

```sh
git add .
git commit -m "Сделал адаптивный header"
git push
```

Если push выполняется впервые из новой ветки:

```sh
git push --set-upstream origin feature/header
```

---

### ✅ 4. Как получить изменения других участников

```sh
git pull
```

---

### ✅ 5. Как объединить работу с `main`

После push ветки на GitHub → зайти на сайт → **Pull Request → Merge**.

Main всегда должен быть стабильной веткой.
Разработка идёт в отдельных ветках `feature/*`.

---

## 🏁 Правила работы в проекте

* **Не работаем напрямую в `main`**
* Каждый новый блок/страницу/фичу — отдельная ветка `feature/...`
* Коммиты должны быть понятными (`"добавил стили футера"`, `"доработан слайдер"`)

---

## 📣 Авторы

| Имя                     | Роль                                |
| ----------------------- | ----------------------------------- |
| *Участник 1* | HTML/CSS/JS, архитектура проекта    |
| *Участник 2* | UI/UX, разработка отдельных страниц |

---

## 🟢 Запуск проекта

Просто открыть `index.html` в браузере.
Сервер не требуется — сайт статический.
